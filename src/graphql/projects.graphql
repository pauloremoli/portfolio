query projects{
  allProject {
    name,
    slug {current},
    image{asset{
      url,
    }},
    description,
    link,
    techologies{
      name,
      logo {asset{url, altText}}
      link
    },
    color {hex}
  }
}
